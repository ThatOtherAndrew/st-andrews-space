---
import BaseLayout from '../layouts/BaseLayout.astro';

const title = 'St Andrews Space';
const description = 'An unoffical MySaint by students, for students.';
const permalink = Astro.site.href;
---

<BaseLayout title={title} description={description} permalink={permalink}>
  <div class="home-container theme-dark">
    <div class="home-copy">
      <h1>{title}</h1>
      <p>{description}</p>
      <a href="/2029" class="new-students-button">New student (class of 2029)? Click here!</a>
    </div>

    <div class="hero-image-container">
      <img class="hero-image" alt={`${title} logo`} src="/favicon.png" width="490" height="490">
    </div>
  </div>

  <div class="body-text">
    <h1>What's this?</h1>
    <hr>
    <p>There is a huge quantity of information out on the internet, and it can be disorienting for students - especially freshers!</p>
    <p>To make matters worse, there is an abundance of <strong>scams</strong> taking advantage of this confusion; for instance, each year <strong>hundreds of fake Freshers' Week tickets</strong> are sold to students. They even <a href="/fatsoma-scam">create the year group chats</a> which hundreds of people join!</p>
    <p>This site aims to collate the <strong>best of the internet</strong> for St Andrews students, and act as a one-stop shop for all St Andrews students on the <strong>unofficial side of the University internet</strong>.</p>
  </div>
</BaseLayout>

<style>
  :global(body) {
    color: #fff;
    background-image: url('/assets/background.jpg');
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    background-repeat: no-repeat;
  }

  .home-container {
    align-items: center;
    display: flex;
    flex: 1;
    justify-content: center;
    margin: 2em 2em 30vh;
    min-height: 60vh;
    filter: drop-shadow(0 10px 20px rgba(0, 0, 0, 0.3));
  }

  .home-copy {
    flex: 1;
    padding: 0 1em;
  }

  .home-copy h1 {
    font-weight: 700;
    margin-bottom: 0.5em;
    line-height: 1.3;
  }

  .home-copy p {
    font-size: 1.4em;
  }

  .hero-image-container {
    margin: 0 1em;
    text-align: center;
  }

  .hero-image-container picture {
    display: block;
    min-height: 250px;
  }

  .caption {
    font-size: .8em;
    font-style: italic;
    text-align: left;
  }

  .hero-image {
    width: 100%;
    max-width: 550px;
    margin-bottom: 1em;
    animation: float 8s ease-in-out infinite;
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  @media (max-width: 1200px) {
    p {
      font-size: 1.2em;
    }

    .hero-image {
      max-width: 320px;
    }
  }

  @media (max-width: 800px) {
    .home-container {
      flex-direction: column;
    }

    .home-copy {
      flex: 0;
      padding-bottom: 2em;
      text-align: center;
    }
  }

  .new-students-button {
    display: inline-block;
    margin-top: 2em;
    padding: .8em 1.4em;
    background-color: var(--primary-color);
    color: #fff;
    text-decoration: none;
    border-radius: 8px;
    font-family: var(--font-family-sans);
    font-size: 1.4em;
    font-weight: 600;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0, 83, 155, 0.3);
  }

  .new-students-button:hover {
    background-color: #003d7a;
  }

  .body-text {
    background-color: rgba(0, 0, 0, 0.7);
    border-radius: 15px;
    padding: 2em;
    margin: 2em;
    backdrop-filter: blur(5px);
    box-shadow: 0 0 10px 10px rgba(0, 0, 0, 0.7);
  }
</style>
